<UserControl x:Class="VToolProMerge.Views.PlaceholderManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgAppBrush}">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title + actions -->
        <Grid>
            <TextBlock Text="Placeholder" Style="{StaticResource H1}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource BtnGhost}" Margin="0,0,8,0"
                        Command="{Binding ImportCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📥" Margin="0,0,6,0"/>
                        <TextBlock Text="Import..."/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource BtnGhost}" Margin="0,0,8,0"
                        Command="{Binding ExportCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📤" Margin="0,0,6,0"/>
                        <TextBlock Text="Export..."/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource BtnPrimary}" Command="{Binding AddCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="➕" Margin="0,0,6,0" Foreground="White"/>
                        <TextBlock Text="Thêm placeholder" Foreground="White"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Stat strip -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="3" Margin="0,16,0,0">
            <Border Style="{StaticResource Card}" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <Border Style="{StaticResource StatIconBox}" Background="{StaticResource BrandPrimarySoftBrush}">
                        <TextBlock Text="{}{ }" FontWeight="Bold" FontSize="18"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrandPrimaryBrush}"/>
                    </Border>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TotalCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="Tổng số placeholder" Style="{StaticResource Caption}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="4,0">
                <StackPanel Orientation="Horizontal">
                    <Border Style="{StaticResource StatIconBox}" Background="{StaticResource WarningSoftBrush}">
                        <TextBlock Text="*" FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="{StaticResource WarningBrush}"/>
                    </Border>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding RequiredCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="Bắt buộc" Style="{StaticResource Caption}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="8,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <Border Style="{StaticResource StatIconBox}" Background="{StaticResource SuccessSoftBrush}">
                        <TextBlock Text="📁" FontSize="20"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding GroupCount}" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="Nhóm" Style="{StaticResource Caption}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Filter chips + search -->
        <Grid Grid.Row="2" Margin="0,16,0,12">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}" Style="{StaticResource BtnChip}"
                                Margin="0,0,8,0"
                                Command="{Binding DataContext.FilterCommand,
                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBox HorizontalAlignment="Right" Width="280"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Tìm theo FieldName hoặc tên hiển thị"/>
        </Grid>

        <!-- DataGrid -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Items}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Token" Binding="{Binding Token}" Width="200" FontFamily="Consolas"/>
                    <DataGridTextColumn Header="FieldName" Binding="{Binding FieldName}" Width="180"/>
                    <DataGridTextColumn Header="Tên hiển thị" Binding="{Binding DisplayName}" Width="*"/>
                    <DataGridTextColumn Header="Nhóm" Binding="{Binding Group}" Width="180"/>
                    <DataGridTextColumn Header="Kiểu" Binding="{Binding DataType}" Width="80"/>
                    <DataGridTextColumn Header="Nguồn" Binding="{Binding Source}" Width="120"/>
                    <DataGridCheckBoxColumn Header="Bắt buộc" Binding="{Binding Required}" Width="80"/>
                    <DataGridTemplateColumn Header="" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑" Width="32" Height="28"
                                        Background="Transparent" BorderThickness="0"
                                        Cursor="Hand"
                                        Foreground="{StaticResource DangerBrush}"
                                        Command="{Binding DataContext.DeleteCommand,
                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
